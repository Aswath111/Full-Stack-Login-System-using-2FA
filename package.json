{
  "name": "full-stack-login-system",
  "version": "1.0.0",
  "description": "Full-stack login system with 2FA using React and Express",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build && cd ../backend",
    "start": "node backend/server.js"
  }
}
