# COPY THIS FILE AND RENAME TO .env (remove .template)
# Then fill in your actual values

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
# For local MongoDB (make sure MongoDB service is running):
MONGO_URI=mongodb://localhost:27017/auth-system

# For MongoDB Atlas (cloud):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/auth-system

# JWT Secret (change this to a random string, at least 32 characters)
JWT_SECRET=my-super-secret-jwt-key-at-least-32-characters-long-please-change-me

# Email Configuration (for OTP sending)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
APP_NAME=Auth System
