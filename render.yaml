services:
  - type: web
    name: full-stack-login
    env: node
    plan: free
    buildCommand: npm run build-all
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
