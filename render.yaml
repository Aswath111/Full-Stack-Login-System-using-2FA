services:
  - type: web
    name: full-stack-login
    env: node
    plan: free
    buildCommand: cd frontend && npm install && npm run build && cd ../backend && npm install && cp -r ../frontend/build . && cd ..
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
